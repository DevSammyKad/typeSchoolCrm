import CreateStudentForm from '@/app/components/dashboard/CreateStudentForm';
import React from 'react';

export const dynamic = 'force-dynamic';

const page = () => {
  return <CreateStudentForm />;
};

export default page;
